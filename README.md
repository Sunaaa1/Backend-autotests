# 🧪 Backend API Automation Tests

**Проект автотестов для REST API.**  
Создан как пример структуры и подхода к backend-автоматизации.  
Тесты написаны с использованием **Python + Pytest + Requests**,  
организованы по принципам **OOP**, с применением **Allure Reports** и **CI/CD через GitHub Actions**.

---

## ⚙️ Используемый стек
- **Python 3.11+**
- **Pytest** — тестовый фреймворк  
- **Requests** — HTTP-клиент для REST API  
- **Allure** — генерация отчетов  
- **JSON Schema** — валидация ответов API  
- **GitHub Actions** — CI/CD для автоматического запуска тестов

---

## 🧩 Структура проекта
- `endpoints/` — классы для работы с API (Create, Get, Update, Delete)  
- `tests/` — сами тесты и фикстуры (`conftest.py`)  
- `schemas/` — JSON-схемы для валидации ответов  
- `.github/workflows/` — CI-конфигурация для GitHub Actions  
- `requirements.txt` — зависимости проекта  

---

## 🚀 Запуск тестов локально

Установи зависимости:
```bash
pip install -r requirements.txt
```

Запусти тесты:
```bash
pytest -v --alluredir=allure-results
```

Сгенерируй отчет Allure:
```bash
allure serve allure-results
```

---

## ☁️ CI/CD (GitHub Actions)
Тесты автоматически запускаются при push или по ручному запуску через GitHub Actions.  
Результаты сохраняются в артефактах пайплайна.

---

## 📚 Цели проекта
- Продемонстрировать навыки API-автоматизации  
- Показать грамотную структуру кода и тестов  
- Использовать проект как **референс для интервью / портфолио**

---

🧑‍💻 **Автор:** [Sunnatilla Shukhratov](https://github.com/Sunaaa1)
